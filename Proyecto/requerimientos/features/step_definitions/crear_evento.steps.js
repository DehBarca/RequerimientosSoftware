const { Given, When, Then } = require('@cucumber/cucumber');

Given('Joey es usuario asistente', function () { return 'pending'; });
Given('Monica es organizadora sin MFA completado', function () { return 'pending'; });
Given('Monica es organizadora autenticada', function () { return 'pending'; });
Given('un evento fue creado exitosamente', function () { return 'pending'; });
Given('el evento se creó correctamente', function () { return 'pending'; });

When('intenta acceder a Crear evento', function () { return 'pending'; });
When('intenta crear un evento', function () { return 'pending'; });
When('intenta crear un evento sin categoría', function () { return 'pending'; });
When('introduce hora de inicio posterior a hora de fin', function () { return 'pending'; });
When('introduce capacidad igual a cero', function () { return 'pending'; });
When('selecciona categoría personalizada aprobada', function () { return 'pending'; });
When('escribe descripción extensa válida', function () { return 'pending'; });
When('guarda el nuevo evento', function () { return 'pending'; });
When('se realiza el registro', function () { return 'pending'; });
When('el sistema habilita los módulos asociados', function () { return 'pending'; });

Then('el sistema niega el acceso', function () { return 'pending'; });
Then('el sistema solicita completar MFA', function () { return 'pending'; });
Then('el sistema solicita seleccionar categoría', function () { return 'pending'; });
Then('el sistema rechaza fecha u horario inválido', function () { return 'pending'; });
Then('el sistema rechaza la capacidad ingresada', function () { return 'pending'; });
Then('el sistema acepta la categoría', function () { return 'pending'; });
Then('el sistema acepta el contenido de la descripción', function () { return 'pending'; });
Then('el sistema registra el evento en estado borrador', function () { return 'pending'; });
Then('el sistema envía notificación al organizador', function () { return 'pending'; });
Then('el evento aparece disponible para el feed público', function () { return 'pending'; });
