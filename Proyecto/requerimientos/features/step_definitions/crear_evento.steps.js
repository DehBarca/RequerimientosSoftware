const { Given, When, Then } = require('@cucumber/cucumber');

Given('el organizador está autenticado', function () {
  return 'pending';
});

When('crea un evento ingresando categoría, horario, capacidad y descripción', function () {
  return 'pending';
});

When('intenta crear un evento sin categoría', function () {
  return 'pending';
});

When('introduce una hora de inicio posterior a la hora de fin', function () {
  return 'pending';
});

Then('el sistema registra el evento correctamente', function () {
  return 'pending';
});

Then('el sistema solicita seleccionar una categoría válida', function () {
  return 'pending';
});

Then('el sistema rechaza la fecha u horario inválido', function () {
  return 'pending';
});
