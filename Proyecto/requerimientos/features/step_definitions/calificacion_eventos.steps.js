const { Given, When, Then } = require('@cucumber/cucumber');

Given('Joey asistió al evento', function () { return 'pending'; });
When('califica el evento con 4 estrellas', function () { return 'pending'; });
Then('el sistema guarda la evaluación', function () { return 'pending'; });

Given('Chandler asistió al evento', function () { return 'pending'; });
When('intenta calificar con 6 estrellas', function () { return 'pending'; });
Then('el sistema rechaza la calificación', function () { return 'pending'; });

Given('Ross ya evaluó ese evento', function () { return 'pending'; });
When('intenta evaluarlo de nuevo', function () { return 'pending'; });
Then('el sistema evita duplicado', function () { return 'pending'; });

Given('Rachel asistió al evento', function () { return 'pending'; });
When('intenta enviar una calificación', function () { return 'pending'; });
Then('el sistema solicita confirmación', function () { return 'pending'; });

Given('Monica es la organizadora del evento', function () { return 'pending'; });
When('intenta evaluarlo', function () { return 'pending'; });
Then('el sistema bloquea su evaluación', function () { return 'pending'; });

Given('Phoebe asistió al evento', function () { return 'pending'; });
When('deja un comentario poético', function () { return 'pending'; });
Then('el sistema lo guarda como comentario opcional', function () { return 'pending'; });

Given('Rachel evaluó el evento', function () { return 'pending'; });
When('su evaluación se procesa', function () { return 'pending'; });
Then('el promedio global se actualiza', function () { return 'pending'; });

Given('el evento no tiene evaluaciones', function () { return 'pending'; });
When('Rachel lo visita', function () { return 'pending'; });
Then('ve “Aún sin calificaciones”', function () { return 'pending'; });
